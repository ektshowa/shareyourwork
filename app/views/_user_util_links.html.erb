  <% if has_user_authentication_provider? %>
    <!--<div class="util-links-login">
      <% if current_user %>
        <%= link_to t('blacklight.header_links.logout'), signout_path %> <%= "[#{ link_to current_user, signup_path }]".html_safe unless current_user.to_s.blank? %>
      <% else %>
        <%= link_to t('blacklight.header_links.login'), signin_path %>
      <% end %>
    </div>-->

  <% end %>

  <div class="util-links-other">
    <% if render_bookmarks_control? %>
      |<%= link_to t('blacklight.header_links.bookmarks'), bookmarks_path %>| <br/>
    <% end %>
    <% if has_user_authentication_provider? and current_user %>
     
     | <%= link_to t('blacklight.header_links.saved_searches'), saved_searches_path %> | <br/>
    <% end %>
   
   | <%= link_to t('blacklight.header_links.search_history'), search_history_path %> |
  </div>
